import { useCallback, useEffect, useState } from "react";
import { AlertTriangle, Check, Circle, ImagePlus, Pencil, Plus, Trash2, Upload, X } from "lucide-react";
import { api, apiError } from "@/api";
import { hindiError, hindiFeedback } from "@/lib/adminFeedback";
import { filterRank } from "@/lib/adminSearch";
import { QC_CHECKS, QC_GRADES } from "@/lib/qc";

const blankProduct = { id: "", name: "", slug: "", sub: "", description: "", category_slug: "mobiles", price: "", original_price: "", stock: "", tag: "NEW", active: true, featured: false, images: [], imageUrl: "", qc_grade: "new", qc_status: {}, imei_number: "", barcode: "" };
const stateOptions = [["pass", "Pass", Check], ["fail", "Defect", AlertTriangle], ["unknown", "Not checked", Circle]];

export default function AdminProductManager({ query = "" }) {
  const [products, setProducts] = useState([]); const [categories, setCategories] = useState([]); const [form, setForm] = useState(blankProduct); const [busy, setBusy] = useState(false);
  const load = useCallback(async () => { try { const [productsRes, categoriesRes] = await Promise.all([api.get("/api/admin/products?page_size=100"), api.get("/api/admin/categories")]); setProducts(productsRes.data.items); setCategories(categoriesRes.data); } catch (error) { hindiError(apiError(error)); } }, []);
  useEffect(() => { load(); }, [load]);
  const set = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  const setQc = (check, value) => setForm((current) => ({ ...current, qc_status: { ...current.qc_status, [check]: value } }));
  const upload = async (event) => { const files = [...(event.target.files || [])]; if (!files.length) return; setBusy(true); try { const urls = await Promise.all(files.map(async (file) => { const data = new FormData(); data.append("file", file); return (await api.post("/api/admin/uploads", data, { headers: { "Content-Type": "multipart/form-data" } })).data.url; })); set("images", [...form.images, ...urls]); hindiFeedback("Product photos added"); } catch (error) { hindiError(apiError(error)); } finally { setBusy(false); event.target.value = ""; } };
  const save = async (event) => { event.preventDefault(); if (!form.images.length) return hindiError("Add at least one product photo"); setBusy(true); try { const payload = { ...form, price: Number(form.price), original_price: Number(form.original_price), stock: Number(form.stock), variants: [] }; if (form.id) await api.patch(`/api/admin/products/${form.id}`, payload); else await api.post("/api/admin/products", payload); hindiFeedback("Product saved and live category updated"); setForm(blankProduct); load(); } catch (error) { hindiError(apiError(error)); } finally { setBusy(false); } };
  const edit = (product) => setForm({ ...blankProduct, ...product, qc_status: product.qc_status || {}, price: String(product.price), original_price: String(product.original_price), stock: String(product.stock), imageUrl: "" });
  const toggleLive = async (product) => { try { await api.patch(`/api/admin/products/${product.id}`, { ...product, active: !product.active }); load(); } catch (error) { hindiError(apiError(error)); } };
  const remove = async (product) => { if (!window.confirm(`Remove ${product.name}?`)) return; try { await api.delete(`/api/admin/products/${product.id}`); load(); } catch (error) { hindiError(apiError(error)); } };
  const visible = filterRank(products, query, [(item) => item.name, (item) => item.slug]);
  return <div className="admin-manager" data-testid="product-manager"><section className="admin-panel product-editor"><div className="panel-head"><div><h2>{form.id ? "Edit live product" : "Add product"}</h2><span>Product category, QC and device ID publish to the storefront immediately.</span></div>{form.id && <button onClick={() => setForm(blankProduct)} data-testid="cancel-product-edit-button"><X size={15} /> Cancel</button>}</div><form onSubmit={save} className="product-form"><input value={form.name} onChange={(event) => { set("name", event.target.value); if (!form.id) set("slug", event.target.value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "")); }} placeholder="Product name" required data-testid="product-name-input" /><input value={form.slug} onChange={(event) => set("slug", event.target.value)} placeholder="Product URL slug" required data-testid="product-slug-input" /><input value={form.sub} onChange={(event) => set("sub", event.target.value)} placeholder="Short details e.g. 256GB · Black" data-testid="product-sub-input" /><label className="product-category-select"><span>Live storefront category · {categories.length} available</span><select value={form.category_slug} onChange={(event) => set("category_slug", event.target.value)} data-testid="product-category-select">{categories.map((category) => <option value={category.slug} key={category.id}>{category.name}</option>)}</select></label><input type="number" min="0" value={form.price} onChange={(event) => set("price", event.target.value)} placeholder="Live price (₹)" required data-testid="product-price-input" /><input type="number" min="0" value={form.original_price} onChange={(event) => set("original_price", event.target.value)} placeholder="Original price (₹)" required data-testid="product-original-price-input" /><input type="number" min="0" value={form.stock} onChange={(event) => set("stock", event.target.value)} placeholder="Stock" required data-testid="product-stock-input" /><input value={form.tag} onChange={(event) => set("tag", event.target.value)} placeholder="Badge e.g. HOT DEAL" data-testid="product-tag-input" /><input value={form.imei_number} onChange={(event) => set("imei_number", event.target.value)} placeholder="IMEI number (optional)" data-testid="product-imei-input" /><input value={form.barcode} onChange={(event) => set("barcode", event.target.value)} placeholder="Barcode (optional)" data-testid="product-barcode-input" /><textarea value={form.description} onChange={(event) => set("description", event.target.value)} placeholder="Product description" data-testid="product-description-input" /><div className="product-media-controls"><label className="upload-zone" data-testid="product-image-upload-label"><Upload size={16} /> Upload photos<input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={upload} disabled={busy} data-testid="product-image-upload-input" /></label><input value={form.imageUrl} onChange={(event) => set("imageUrl", event.target.value)} placeholder="Image URL" data-testid="product-image-url-input" /><button type="button" onClick={() => { if (/^https?:\/\//.test(form.imageUrl)) setForm((current) => ({ ...current, images: [...current.images, current.imageUrl], imageUrl: "" })); }} data-testid="add-product-image-url-button"><Plus size={14} /></button></div><div className="product-image-previews" data-testid="product-image-previews">{form.images.map((url, index) => <div className="product-image-thumb" key={`${url}-${index}`}><img src={url} alt="Product" /><button type="button" onClick={() => set("images", form.images.filter((_, row) => row !== index))} data-testid={`remove-product-image-${index}`}><X size={13} /></button></div>)}{!form.images.length && <span className="image-empty"><ImagePlus size={16} /> Add product photos</span>}</div><div className="qc-editor" data-testid="product-qc-editor"><div className="qc-editor-head"><div><b>Quality Check</b><small>Select actual device condition.</small></div></div><div className="qc-grade-tabs" data-testid="product-qc-grade-select">{QC_GRADES.map((grade) => <button type="button" className={form.qc_grade === grade.value ? "active" : ""} onClick={() => set("qc_grade", grade.value)} key={grade.value} data-testid={`product-qc-grade-${grade.value}`}>{grade.label}</button>)}</div><div className="qc-check-grid">{QC_CHECKS.map((check) => <div className="qc-check-item" key={check}><b>{check}</b><div className="qc-toggle">{stateOptions.map(([value, title, Icon]) => <button type="button" title={title} className={form.qc_status[check] === value ? `on ${value}` : ""} onClick={() => setQc(check, value)} key={value} data-testid={`qc-${check}-${value}`}><Icon size={13} /></button>)}</div></div>)}</div></div><label className="publish-check"><input type="checkbox" checked={form.active} onChange={(event) => set("active", event.target.checked)} data-testid="product-publish-checkbox" /> Publish on website</label><button className="primary-btn" disabled={busy} data-testid="save-product-button"><Check size={15} /> {busy ? "Saving…" : form.id ? "Update product" : "Publish product"}</button></form></section><section className="admin-panel table-panel admin-wide"><div className="panel-head"><div><h2>Live product catalog</h2><span>{visible.length} matching products</span></div></div>{visible.length ? <div className="product-admin-grid">{visible.map((product) => <article className="admin-product-card" key={product.id} data-testid={`admin-product-${product.id}`}><img src={product.images?.[0]} alt={product.name} /><div><span className={product.active ? "status-live" : "status-draft"}>{product.active ? "LIVE" : "DRAFT"}</span><h3>{product.name}</h3><p>{product.category_slug} · ₹{Number(product.price).toLocaleString("en-IN")} · {product.stock} stock</p></div><div className="admin-product-actions"><button onClick={() => edit(product)} data-testid={`edit-product-${product.id}`}><Pencil size={14} /> Edit</button><button onClick={() => toggleLive(product)} data-testid={`publish-product-${product.id}`}>{product.active ? "Unpublish" : "Publish"}</button><button onClick={() => remove(product)} data-testid={`delete-product-${product.id}`}><Trash2 size={14} /></button></div></article>)}</div> : <p className="admin-empty-copy" data-testid="product-search-empty">No product matches “{query}”.</p>}</section></div>;
}